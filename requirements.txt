# Production pipeline runs on Databricks/Fabric (PySpark + Delta provided by the runtime).
pyspark>=3.5
delta-spark>=3.2

# Local reference engine + tests only:
pandas>=2.0
pyyaml>=6.0
# pytest>=7.0   # optional; tests also run via: python3 tests/test_recon.py
